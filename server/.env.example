# اتصال قاعدة البيانات (Railway يضبطه تلقائياً عند ربط خدمة PostgreSQL)
DATABASE_URL=postgres://hajiz:hajiz@localhost:5432/hajiz
PORT=3000
# لقاعدة الاختبارات فقط
TEST_DATABASE_URL=postgres://hajiz:hajiz@localhost:5432/hajiz_test

# حجز

تطبيق حجز مواعيد للمحلات في البصرة. القرارات المتفق عليها في مستند «تطبيق الحجز: القرارات المتفق عليها».

| المجلد | المحتوى |
| --- | --- |
| `server/` | الخادم: Node.js + TypeScript + PostgreSQL |
| `app/` | تطبيق الزبون: Flutter (أندرويد وآيفون) |

## خطة البناء

| المرحلة | ماذا نبني | الحالة |
| --- | --- | --- |
| 1 | قاعدة البيانات والخادم الأساسي، ونشره على المنصة | اكتملت: يعمل على hajiz-production.up.railway.app |
| 2 | محرك الحجز: الأوقات المتاحة، المهل، الإلغاء التلقائي | اكتملت |
| 3 | تطبيق الزبون: التصفح والحجز وحجوزاتي | اكتملت: النسخة التجريبية تُبنى على GitHub |
| 4 | واجهة صاحب المحل وطلب فتح المحل | اكتملت |
| 5 | الإشعارات | اكتملت: تعمل داخل التطبيق، وتصل للهاتف بعد إعداد Firebase |
| 6 | لوحة الإدارة | لم تبدأ |
| 7 | تجربة مع محلات في البصرة، ثم النشر | لم تبدأ |

## تشغيل الخادم محلياً

يحتاج Node.js 22 و PostgreSQL 16.

```sh
cd server
npm install
cp .env.example .env        # ثم عدّل DATABASE_URL
export $(cat .env | xargs)
npm run migrate:dev          # إنشاء الجداول والقيم الابتدائية
npm run dev                  # http://localhost:3000/health
npm test                     # يحتاج قاعدة hajiz_test فارغة
```

كل مسارات التطبيق تحت `/v1` وتحتاج الترويستين `X-App-Platform` (android أو ios أو web) و `X-App-Version`.
إذا كانت النسخة أقدم من أقل إصدار مسموح يرد الخادم برمز 426 `update_required`.

| المسار | الوظيفة |
| --- | --- |
| `GET /health` | فحص الخادم وقاعدة البيانات |
| `GET /v1/config` | الإعدادات والقوائم ووقت الخادم |
| `GET /v1/shops/:shopId/slots?date=2026-10-05&services=3,7` | الأوقات المتاحة ليوم واحد |
| `GET /v1/shops`، `GET /v1/shops/:shopId` | البحث وصفحة المحل |
| `POST /v1/auth/register` | الاسم والرقم عند أول حجز، ويعيد مفتاح الجهاز |
| `POST /v1/bookings`، `GET /v1/me/bookings`، `GET /v1/bookings/:id` | الحجز وحجوزاتي (تحتاج `Authorization: Bearer`) |
| `POST /v1/bookings/:id/cancel`، `choose`، `decline` | الإلغاء والرد على اقتراح المحل |
| `/v1/owner/...` | طلب فتح المحل، الخدمات، الأوقات، الإعدادات، الصور، الإغلاق الطارئ، لوحة المحل والرد على الطلبات |
| `GET /photos/:id` | صور المحلات |

إدارة المحل متاحة فقط من الجهاز الموثوق (أول جهاز سُجّل به الرقم أو جهاز أكدته الإدارة).

طلبات `POST` على الحجوزات تحتاج `Idempotency-Key` فريداً لمنع الإرسال المكرر.
`SEED_DEMO=true` يُنشئ «صالون تجريبي» مرة واحدة لتجربة التطبيق.

محرك الحجز في `server/src/booking/`: حساب الأوقات، المهل، تغييرات حالة الحجز، والمهام التلقائية التي تعمل كل دقيقة
(إلغاء الطلبات منتهية المهلة، والإكمال التلقائي).

الأخطاء تُرسل برمز ثابت فقط مثل `{"error":{"code":"not_found"}}`، والتطبيق يترجمه من ملف الترجمة.

## النشر على Railway

1. أنشئ مشروعاً على Railway وأضف له خدمة PostgreSQL.
2. أضف خدمة من هذا المستودع واضبط **Root Directory** على `server`.
3. في متغيرات الخدمة أضف `DATABASE_URL` مرتبطاً بمتغير قاعدة البيانات: `${{Postgres.DATABASE_URL}}`.
4. الخادم يطبّق الترحيلات بنفسه عند كل تشغيل، والإعداد في `server/railway.json` يبني المشروع ويفحص `/health`.
5. فعّل النسخ الاحتياطي اليومي من إعدادات خدمة PostgreSQL.

## الإشعارات (Firebase)

بدون Firebase تُحفظ الإشعارات وتظهر في جرس الإشعارات داخل التطبيق فقط. لتصل إلى الهاتف والتطبيق مغلق:

1. أنشئ مشروعاً على [Firebase](https://console.firebase.google.com) وأضف له تطبيق أندرويد باسم الحزمة `iq.hajiz.hajiz`.
2. حمّل ملف `google-services.json` وضعه في `app/android/app/` (ليس سرياً، يُحفظ في المستودع).
3. من إعدادات المشروع ← Service accounts ← Generate new private key: هذا الملف **سري**. انسخ محتواه كاملاً إلى متغير `FIREBASE_SERVICE_ACCOUNT` في خدمة الخادم على Railway، ولا تضعه في المستودع.
4. سجل الخادم عند التشغيل يقول `Push notifications: Firebase` عندما يكون الإعداد صحيحاً.

الإشعارات المهمة (طلب جديد، قبول، رفض، اقتراح وقت، إلغاء، انتهاء المهلة) لا تُوقف. التذكيرات (منتصف المهلة، قبل انتهائها بـ 5 دقائق، قبل الموعد بساعة) وملخص الصباح (8:00 لصاحب المحل) يوقفها المستخدم من إعدادات الإشعارات.
